#include<stdio.h>
int main()
{
    int n,sum=0,i;
    printf("Enter the number to find its sum of factors :");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(n%i==0){
            sum=sum+i;
        }
    }   
    printf ("Sum of factors  = %d",sum);
   return 0;
}
